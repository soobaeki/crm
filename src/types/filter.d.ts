export interface CustomerSearchFilter {
  startDate: string;
  endDate: string;
  searchText: string;
}

export interface IExcelSearchFilter {
  startDate: string;
  endDate: string;
  searchText: string;
  item: string;
  weight: number;
}
